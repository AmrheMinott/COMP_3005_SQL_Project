{
  "name": "comp3005_project",
  "version": "1.0.0",
  "description": "This is a web application where we query a postgresql server using javascript as the main programming language but this is done via. Node express and pg on our server.js we are making sql queries to sed server. This accesses our bookstore database where additions to the server of books, users and orders.",
  "main": "book_store.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amrhe Minott",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "pg": "^8.0.0",
    "util": "^0.12.2",
    "uuid": "^7.0.3"
  }
}
